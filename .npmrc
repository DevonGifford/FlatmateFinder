# React 19 experiment: react-tinder-card and the current generated UI peers still advertise React 18.
# Remove this once the shadcn/Radix refresh lands or the Tinder dependency is replaced.
legacy-peer-deps=true
